import AtlaspackPlugin from '@atlaspack/plugin/src/PluginAPI.js'
const { Resolver } = /** @type {import('@atlaspack/plugin/src/PluginAPI')} */ (AtlaspackPlugin)

export default new Resolver({
  resolve({}) {
    return null
  }
})
